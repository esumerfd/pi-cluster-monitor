---
all:
  children:
    pi_nodes:
      hosts:
        control:
          ansible_host: 192.168.1.10
        worker1:
          ansible_host: 192.168.1.11
        worker2:
          ansible_host: 192.168.1.12
      vars:
        ansible_user: pi
        ansible_ssh_private_key_file: ~/.ssh/id_ed25519
        # Override defaults here if needed:
        # pi_agent_version: "v0.2.0"
        # pi_agent_port: 8765
